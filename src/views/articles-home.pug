extends basic.pug
block content

    h1 Список всех ваших статей
    a(href="/articles") Можете добавить новую статью тут.
    p
        a(href="/user") Личный кабинет
    each article in articles
        div(class="card d-flex justify-content-center")
            div(class="card-header") #{article.title}
                a(href="edit/" + article._id) (ред.)    
            div(class="card-body") #{article.content}